<h1> All <%= @merchant.name %>'s Invoices </h1>

<% @invoices.each do |invoice| %>
  <div id="invoice-<%=invoice.id%>">
  <p> Invoice ID: <%= link_to invoice.id, merchant_invoice_path(@merchant, invoice) %> </p>
<%end%>
