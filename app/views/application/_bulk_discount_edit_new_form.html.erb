<%= form_with model: @bulk_discount, url: merchant_bulk_discount_path do |f| %>
  <%= f.label :discount, 'Discount %' %>
  <%= f.text_field :discount %>

  <%= f.label :threshold %>
  <%= f.text_field :threshold %>

  <%= f.submit %>
<% end %>